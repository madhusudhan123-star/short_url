const http = require('http');
const fs = require('fs');
const path = require('path');
const PORT = 4000;
const url = require('url');
const express = require('express');


module.exports = {
    http,
    fs,
    path,
    url,
    PORT,
    express
};